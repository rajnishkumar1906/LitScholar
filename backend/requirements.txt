# Core web/API – pinned where stable
fastapi==0.129.0
uvicorn[standard]==0.34.0
pydantic>=2.7.0,<3.0.0

# File & image handling
python-multipart==0.0.20
pillow==12.1.1  # Updated from 11.1.0 (version didn't exist)

# Supabase client
supabase==2.28.0

# Database driver
psycopg[binary]==3.1.18  # Pinned for stability
asyncpg==0.31.0  # Updated from 0.29.0 (version didn't exist)

# Authentication & JWT
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pyjwt>=2.10.1
authlib==1.6.8
bcrypt==4.1.3  # Added explicitly

# LangChain & related
langchain==1.2.10
langchain-community==0.4.1
langchain-core==1.2.13

# Embeddings & models
transformers==4.44.0
sentence-transformers==3.3.0
numpy==2.2.0
torch==2.5.1  # Added for transformers
torchvision==0.20.1  # Added for transformers

# Tokenizers - pinned for pre-built wheels (avoids Rust compilation)
tokenizers==0.19.1

# Utils
requests==2.32.5
python-dotenv==1.0.1
httpx==0.27.2  # Added for async HTTP

# Vector store
chromadb==1.5.0
click==8.1.7  # ChromaDB dependency
overrides==7.7.0  # ChromaDB dependency

# Google AI
google-genai==1.64.0
google-generativeai==0.8.4  # Added for Gemini

# Optional / commented out for now
# ollama==0.6.1
